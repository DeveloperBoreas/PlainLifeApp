<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@mipmap/background"
        tools:context=".mvp.views.activitys.MainActivity">

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/navigationView"
            android:layout_width="match_parent"
            android:layout_height="@dimen/main_bottomNavigtion_height"
            android:background="@drawable/main_navigation_shape"
            app:itemIconTint="@drawable/selector_bottom_navigation_icon"
            app:itemTextColor="@drawable/selector_bottom_navigation_text"
            app:layout_constraintBottom_toBottomOf="parent"
            app:menu="@menu/navigation" />

        <com.boreas.plainlife.widget.viewpager.ViewPagerSlide
            android:id="@+id/container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_constraintBottom_toTopOf="@+id/navigationView" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <data>

    </data>
</layout>